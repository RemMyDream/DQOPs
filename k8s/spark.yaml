apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-master
  namespace: dqops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spark-master
  template:
    metadata:
      labels:
        app: spark-master
    spec:
      containers:
      - name: spark-master
        image: apache/spark:3.5.3
        env:
        - name: SPARK_MODE
          value: "master"
        - name: SPARK_MASTER_BIND_ADDRESS #cause we run KinD: container's network interface typically only has the Pod IP (e.g., 10.244.x.x) and localhost. It does not "own" the ClusterIP, so the OS rejects the bind request.
          value: "0.0.0.0" 
        - name: SPARK_MASTER_HOST
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_MASTER_HOST
        - name: SPARK_MASTER_PORT
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_MASTER_PORT
        - name: SPARK_MASTER_WEBUI_PORT
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_MASTER_WEBUI_PORT
        command:
        - /opt/spark/bin/spark-class
        - org.apache.spark.deploy.master.Master
        ports:
        - containerPort: 7077
          name: spark
        - containerPort: 8080
          name: web-ui
        resources:
          requests:
            memory: "1Gi"
            cpu: "200m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: spark-master
  namespace: dqops
spec:
  selector:
    app: spark-master
  ports:
  - name: webui
    port: 8080
    targetPort: 8080
  - name: master
    port: 7077
    targetPort: 7077
  - name: app-ui
    port: 4040
    targetPort: 4040
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-worker-1
  namespace: dqops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spark-worker
      worker-id: "1"
  template:
    metadata:
      labels:
        app: spark-worker
        worker-id: "1"
    spec:
      containers:
      - name: spark-worker
        image: apache/spark:3.5.3
        env:
        - name: SPARK_MODE
          value: "worker"
        - name: SPARK_MASTER_URL
          value: "spark://spark-master:7077"
        - name: SPARK_WORKER_CORES
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_WORKER_CORES
        - name: SPARK_WORKER_MEMORY
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_WORKER_MEMORY
        - name: SPARK_WORKER_WEBUI_PORT
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_WORKER_WEBUI_PORT
        command:
        - /opt/spark/bin/spark-class
        - org.apache.spark.deploy.worker.Worker
        - spark://spark-master:7077
        ports:
        - containerPort: 8081
        resources:
          requests:
            memory: "2Gi"
            cpu: "1"
          limits:
            memory: "4Gi"
            cpu: "2"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-worker-2
  namespace: dqops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spark-worker
      worker-id: "2"
  template:
    metadata:
      labels:
        app: spark-worker
        worker-id: "2"
    spec:
      containers:
      - name: spark-worker
        image: apache/spark:3.5.3
        env:
        - name: SPARK_MODE
          value: "worker"
        - name: SPARK_MASTER_URL
          value: "spark://spark-master:7077"
        - name: SPARK_WORKER_CORES
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_WORKER_CORES
        - name: SPARK_WORKER_MEMORY
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_WORKER_MEMORY
        - name: SPARK_WORKER_WEBUI_PORT
          valueFrom:
            configMapKeyRef:
              name: spark-config
              key: SPARK_WORKER_WEBUI_PORT
        command:
        - /opt/spark/bin/spark-class
        - org.apache.spark.deploy.worker.Worker
        - spark://spark-master:7077
        ports:
        - containerPort: 8081
        resources:
          requests:
            memory: "2Gi"
            cpu: "1"
          limits:
            memory: "4Gi"
            cpu: "2"
---
apiVersion: v1
kind: Service
metadata:
  name: spark-workers
  namespace: dqops
spec:
  selector:
    app: spark-worker
  ports:
  - port: 8081
    targetPort: 8081
  type: ClusterIP

